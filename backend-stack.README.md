          (1) docker push
개발/CI ───────────────────────► [ ECR (이미지 저장소) ]
                                     ▲
                                     │ (6) ECS가 pull
                                     │
┌──────────────────────────────────────────────────────────────────┐
│                 [ ECS Cluster (논리적 실행 풀) ]                  │
│                                                                  │
│   ┌──────────────────────┐           ┌────────────────────────┐  │
│   │  ECS Service          │  (4)      │  Capacity Provider     │  │
│   │  - desired count 유지 │──────────►│  - ASG 용량 연동       │  │
│   │  - task 재기동/배포    │           └───────────▲────────────┘  │
│   └───────────▲──────────┘                       │ (5) 부족하면
│               │ (3) task 배치                    │     ASG 확장
│               │                                   │
│   ┌───────────┴───────────────────────────────────┴───────────┐  │
│   │         EC2 Container Instances (컨테이너가 실제 도는 서버) │  │
│   │  [EC2 #1]    [EC2 #2]    [EC2 #3] ...                      │  │
│   │    │            │            │                             │  │
│   │   Task         Task         Task   (컨테이너 실행 단위)     │  │
│   └───────────▲────────────────────────────────────────────────┘  │
└───────────────│───────────────────────────────────────────────────┘
                │ (2) 서버를 찍어냄
        ┌───────┴───────────┐
        │   Auto Scaling     │
        │   Group (ASG)      │
        └───────▲───────────┘
                │
        ┌───────┴───────────┐
        │ Launch Template    │
        │ (AMI/타입/SG/UserData:
        │  ECS_CLUSTER=...)  │
        └───────────────────┘
